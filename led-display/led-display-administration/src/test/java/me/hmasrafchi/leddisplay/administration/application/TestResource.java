/**
 * 
 */
package me.hmasrafchi.leddisplay.administration.application;

/**
 * @author michelin
 *
 */
public class TestResource {
	// Response sendPostCreateScene(final WebTarget webTarget) {
	// final SceneComposite scene = new SceneComposite();
	// return
	// webTarget.path("scenes").request(MediaType.APPLICATION_JSON).post(Entity.json(scene));
	// }
	//
	// SceneComposite getScene(final WebTarget webTarget, final String path) {
	// return (SceneComposite)
	// webTarget.path(path).request(MediaType.APPLICATION_JSON).get().readEntity(Scene.class);
	// }
	//
	// String getWebTargetPath(final Response response) {
	// final URI location = response.getLocation();
	// final String locationPath = location.getPath();
	// final String locationPathWithoutLeading = locationPath.substring(1);
	//
	// return
	// locationPathWithoutLeading.substring(locationPathWithoutLeading.indexOf("/")
	// + 1);
	// }
}